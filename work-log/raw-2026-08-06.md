# 工作日志 2026-08-06

---
### [11:15]
**来源：** Kimi
**问题：** 修改卡牌合成规则：牌堆中存在配方/组合之外的多余卡牌时不触发合成进度。
**解决：** 排查发现 `StacklandsWorkCtrl.RequirementMatches` 把匹配方式与字符串 `"CARD"` 比较，而 Luban 运行时实际值为 `"EXACT"`（`ECardMatcher.ToString()`），导致全部 69 个配方及宝箱/商车/神庙等带原料需求的动作从未能触发；仅无原料需求的采集/探索动作可触发，且无视堆中多余卡牌。已修复：`RequirementMatches` 支持 `EXACT`/`TAG`/`CATEGORY`；配方维持精确匹配（所有配方 `allow_extra_cards=0`，牌堆卡牌数须恰等于需求总数）；`MatchesAction` 改为精确组成（除来源卡外仅允许原料 + 至多一张工人卡，工人卡计入原料时不重复计数，兼容 Old Tome 研究）。匹配逻辑已用独立控制台程序加载真实 Luban 二进制逐场景验证（精确堆触发、多余卡牌阻断、宝箱/商车动作恢复）。已新增 5 个 EditMode 测试（配方精确触发/多余阻断、动作精确触发/多余阻断、宝箱 EXACT 需求触发）；编辑器内编译通过，但命令行批处理跑测试两次均异常提前退出（疑与本机 Test Framework CLI 有关，此前成功测试系经 unity-mcp 在编辑器内执行），完整 EditMode 回归待编辑器内验证。另发现既有 bug 未修：`action_study_old_tome` 要求 `VILLAGER` 工人，但无卡牌带 `VILLAGER` 标签，该动作从未能触发。
**涉及文件：** UnityProject/Assets/GameScripts/HotFix/GameLogic/Core/Ctrl/StacklandsWorkCtrl.cs、UnityProject/Assets/Tests/EditMode/StacklandsModelLoaderTests.cs、Books/游戏核心代码说明.md
---

---
### [11:20]
**来源：** Kimi
**问题：** 牌堆中移除多余卡牌后，合成进度没有开始。
**解决：** 定位为 `StacklandsBoardCtrl.Move` 只对被移动卡牌落入的牌堆尝试触发合成/动作，拆堆后源牌堆剩余卡牌不会重新评估。已抽出 `TryStartWork`（依次尝试装备、配方、动作），并在源牌堆仍有剩余卡牌时对其补一次评估；新增 EditMode 测试 `Recipe_RemovingExtraCardFromStack_StartsWork` 覆盖该场景。编辑器内两个程序集重新编译无错误（用户编辑器处于打开状态，命令行回归未执行）。
**涉及文件：** UnityProject/Assets/GameScripts/HotFix/GameLogic/Core/Ctrl/StacklandsBoardCtrl.cs、UnityProject/Assets/Tests/EditMode/StacklandsModelLoaderTests.cs、Books/游戏核心代码说明.md
---

---
### [11:35]
**来源：** Kimi
**问题：** 修改合成中卡牌无法再堆叠卡牌的逻辑，改成堆叠后合成进度终止；要求重构而非补丁式修改。
**解决：** 重新梳理工作进度生命周期（创建/终止/完成）后重构：`BoardCtrl.Move` 拆分为空间平移快路径、`TryRestack`（容量校验、终止相关工作、重组牌堆）与双堆重新评估三个阶段，删除工作中牌堆拒绝新卡的 `HasActiveWork` 分支——现在向工作中的牌堆堆叠会接受新卡并立即终止其进度；工作终止谓词收拢为 `WorkCtrl.TerminateWorksInvolving`，删除 `Model.CancelWorks`（`RemoveCard` 内联单卡清理保持状态一致性）。旧测试 `WorkingStack_PreservesProgressAndRejectsNewCards` 拆为 `WorkingStack_SpatialMove_PreservesProgress` 与 `WorkingStack_StackingNewCard_TerminatesProgress` 以匹配新行为。编辑器内重编译无错误。
**涉及文件：** UnityProject/Assets/GameScripts/HotFix/GameLogic/Core/Ctrl/StacklandsBoardCtrl.cs、UnityProject/Assets/GameScripts/HotFix/GameLogic/Core/Ctrl/StacklandsWorkCtrl.cs、UnityProject/Assets/GameScripts/HotFix/GameLogic/Core/Model/StacklandsGameModel.cs、UnityProject/Assets/Tests/EditMode/StacklandsModelLoaderTests.cs、Books/游戏核心代码说明.md
---
